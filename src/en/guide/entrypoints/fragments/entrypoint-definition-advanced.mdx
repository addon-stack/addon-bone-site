:::info Advanced (explicit exports)
Use this format when you already know the available fields and their types.
Options are declared via named exports, and the target handler is declared via `export default`.
:::
