import {Persistent as BasePersistent} from "@components/docs/entrypoints";

export default function Persistent() {
	return (
		<BasePersistent>
			Controls persistent behavior for background context (relevant for MV2
			background page mode).
		</BasePersistent>
	);
}
